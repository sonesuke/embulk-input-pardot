in:
  type: pardot
  user_name: {{ env.PARDOT_USER_NAME }}
  password: {{ env.PARDOT_PASSWORD }}
  user_key: {{ env.PARDOT_USER_KEY }}
  timezone: Asia/Tokyo
  updated_after: last_7_days
  object: prospect
out:
  type: stdout